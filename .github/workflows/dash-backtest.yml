# بک‌تست موتور داشبورد ۲.۰ (سوینگ ۴س/۱س/۱۵د + دروازهٔ بازار) روی کندل واقعی.
# دستور حمید (۲۰ اوت): کار سنگین را پایتون روی Actions انجام دهد، نه نشست.
# ادعای عملکرد فقط از این‌جا بیرون می‌آید (عدد بی‌بازتولید گزارش نمی‌شود).
name: Dash backtest

on:
  workflow_dispatch:
    inputs:
      symbols:
        description: "چند نماد"
        default: "120"
      bars:
        description: "چند کندل ۱۵ دقیقه (سقف امن صرافی‌ها ۱۰۰۰)"
        default: "1000"
  schedule:
    - cron: "23 4 * * *"        # روزی یک بار، دقیقهٔ فرد (کرون */N ممنوع)

permissions:
  contents: write

concurrency:
  group: dash-backtest
  cancel-in-progress: false

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - name: Self-test
        working-directory: claude-liam-signal/python
        run: |
          python3 -m hamid.test_dash_backtest
          python3 -m hamid.test_exec_contract
      - name: Backtest on real candles
        working-directory: claude-liam-signal/python
        run: |
          set -o pipefail
          python3 -m hamid.dash_backtest \
            --symbols "${{ github.event.inputs.symbols || '120' }}" \
            --bars "${{ github.event.inputs.bars || '1000' }}" | tee dash.txt
      - name: Summary
        if: always()
        working-directory: claude-liam-signal/python
        run: |
          { echo '```'; tail -30 dash.txt 2>/dev/null || echo "no output"; echo '```'; } \
            >> $GITHUB_STEP_SUMMARY
      - name: Publish
        run: |
          git config user.name "Hamid Signal Agent"
          git config user.email "actions@users.noreply.github.com"
          git add -A signals
          git diff --cached --quiet && { echo "بدون تغییر"; exit 0; }
          git commit -q -m "بک‌تست داشبورد $(date -u '+%Y-%m-%d %H:%M') UTC"
          for attempt in 1 2 3 4 5 6 7 8; do
            git push origin HEAD:main && exit 0
            git fetch origin main || true
            if ! git merge --no-edit origin/main; then
              python3 "$GITHUB_WORKSPACE/scripts/resolve_brain_conflicts.py" \
                || { git merge --abort; exit 1; }
              git commit --no-edit || { git merge --abort; exit 1; }
            fi
            sleep $((attempt * 4 + RANDOM % 7))
          done
          exit 1
