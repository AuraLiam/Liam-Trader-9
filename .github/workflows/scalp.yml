# میز اسکلپ ۱ دقیقه — دستور حمید ۱۸ اوت. دقایق فرد (درس اندازه‌گیری‌شده:
# کرون‌های */N شلوغ‌اند و جاب می‌افتد). هر نوبت: ریپلی ۱m + ستاپ زنده + پنل.
name: Scalp desk (1m)

on:
  schedule:
    - cron: "3,18,33,48 * * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: scalp-desk
  cancel-in-progress: false

jobs:
  scalp:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }
      - uses: actions/setup-python@v5
        with: { python-version: "3.11" }
      - name: Self-test
        working-directory: claude-liam-signal/python
        run: python3 -m hamid.test_scalp
        # پاسبانِ ماشینِ حکم — همراه اجباریِ scalp_verdict
      - name: Self-test (حکم)
        working-directory: claude-liam-signal/python
        run: python3 -m hamid.test_scalp_verdict
        # پاسبان موتور قطعیِ ۱د + کانال آپدیت تحلیل
      - name: Self-test (موتور ۱د)
        working-directory: claude-liam-signal/python
        run: python3 -m hamid.test_scalp1m
      - name: Run scalp desk
        working-directory: claude-liam-signal/python
        run: python3 -m hamid.scalp
      # پل اسکلپ→داشبورد (دستور حمید ۱۹ اوت): ستاپِ مناسبِ اسکلپ فوری
      # به سفارش فیوچرز امضاشده تبدیل می‌شود. mode=demo؛ پول واقعی فقط با
      # LIAM9_ALLOW_LIVE=1 روی ماشین خود حمید.
      - name: پل اجرا به داشبورد
        working-directory: claude-liam-signal/python
        env:
          LIAM9_LINK_SECRET: ${{ secrets.LIAM9_LINK_SECRET }}
        run: python3 -m hamid.scalp_exec --mode demo || true

      # ماشینِ حکم (دستور حمید ۲۴ اوت: «این سیستم یک دقیقه‌ای باید به
      # صورت مجزا برویم جلو تا به نتیجه برسد»). هر نوبت می‌گوید
      # PROMOTE / REJECT / UNDECIDED و چقدر تا تصمیم‌پذیری مانده.
      # آلارم فقط روی **تغییر حکم** می‌رود، نه هر ۱۵ دقیقه.
      - name: حکم میز ۱ دقیقه
        working-directory: claude-liam-signal/python
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python3 -m hamid.scalp_verdict --alert || true
      - name: Publish
        run: |
          git config user.name "Hamid Signal Agent"
          git config user.email "actions@users.noreply.github.com"
          git add -A brain/paper signals
          git diff --cached --quiet && { echo "بدون تغییر"; exit 0; }
          git commit -q -m "میز اسکلپ $(date -u '+%Y-%m-%d %H:%M') UTC"
          for attempt in 1 2 3 4 5 6 7 8; do
            git push origin HEAD:main && break
            git fetch origin main || true
            if ! git merge --no-edit origin/main; then
              python3 "$GITHUB_WORKSPACE/scripts/resolve_brain_conflicts.py" \
                || { git merge --abort; exit 1; }
              git commit --no-edit || { git merge --abort; exit 1; }
            fi
            sleep $((attempt * 4 + RANDOM % 7))
          done
      - name: Publish to panel (gh-pages)
        run: |
          git fetch origin gh-pages
          rm -rf /tmp/ghp && git worktree add /tmp/ghp origin/gh-pages --detach
          mkdir -p /tmp/ghp/signals
          for f in scalp link-commands scalp-exec live-link; do
            cp "signals/$f.json" /tmp/ghp/signals/ 2>/dev/null || true
          done
          cd /tmp/ghp
          git add signals
          git diff --cached --quiet && { echo "unchanged"; exit 0; }
          git commit -q -m "اسکلپ $(date -u '+%H:%M') UTC"
          for a in 1 2 3 4 5 6 7 8; do
            git push origin HEAD:gh-pages && exit 0
            git fetch origin gh-pages || true
            git merge -X ours -m "ادغام gh-pages" origin/gh-pages || { git merge --abort 2>/dev/null || true; }
            sleep $((a * 4 + RANDOM % 7))
          done
          exit 1
