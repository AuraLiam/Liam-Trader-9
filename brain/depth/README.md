# دادهٔ عمق — ویژگی‌های E10

هر فایل `<SYMBOL>.jsonl` عکس‌های ویژگی‌شدهٔ دفتر سفارش است (نه دفتر خام).
تولید: `hamid/depth_collector.py` از طریق ورک‌فلوی `depth-collect.yml`.

**این OFI واقعی نیست** — نمونه‌برداری REST است، نه استریم رویداد. فیلدهای
تغییر با `d_` شروع می‌شوند تا این تمایز در خود داده بماند (قانون ۰۸).
لغو و اجرا بین دو عکس از هم قابل‌تفکیک نیستند، پس ادعای spoofing با این
داده ناممکن است.

تا وقتی حجم داده کافی نشده، هیچ ویژگی‌ای وارد دروازهٔ سیگنال نمی‌شود
(چرخهٔ قانون ۰۳: OBSERVATION → ... → BACKTEST → CI بالای صفر).
