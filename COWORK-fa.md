# کار با Cowork (کلود روی لپ‌تاپ خودت) — به زبان ساده 🖥

## Cowork چیست و محیطش جداست یا نه؟

Cowork همان کلود است، ولی **روی لپ‌تاپ خودت** اجرا می‌شود، نه در فضای
ابری. فرقش فقط «دسترسی» است:

| | کلود ابری (همین چت) | Cowork (لپ‌تاپ) |
|---|---|---|
| فایل‌های لپ‌تاپت (مثل `C:\AuraLiamData`) | ❌ نمی‌بیند | ✅ می‌بیند |
| ۲۴ ساعته بیدار | ✅ (گیت‌هاب) | فقط تا وقتی لپ‌تاپ روشن است |
| حافظه و قوانین پروژه | همین ریپو | **همین ریپو** — مشترک |

جواب سؤالت: **محیط جدا دارد ولی مغز جدا ندارد.** هر دو کلود همین ریپو را
می‌خوانند (قوانین، حافظه، کدها). پل ارتباطی‌شان گیت‌هاب است: من از این‌جا
کد و قانون می‌گذارم، Cowork روی لپ‌تاپت `git pull` می‌کند و همان را دارد؛
او هم اگر چیزی ساخت push می‌کند و من می‌بینم. برای همین گیت‌هاب سر جایش
می‌ماند — او دفترِ مشترک ماست، نه یک شلوغی اضافه.

## راه‌اندازی — یک بار (اگر قبلاً ریپو را کلون کرده‌ای، فقط قدم ۳)

1. برنامهٔ **Claude Desktop** را نصب و وارد همان اکانتت شو.
2. اگر ریپو روی لپ‌تاپ نیست: (پوشهٔ دلخواه، مثلاً `hamid-live`)
   ```
   git clone https://github.com/Auraliam/liam-trader-9.git
   ```
3. در Claude Desktop حالت **Cowork** را باز کن و **پوشهٔ ریپو** را به
   عنوان فضای کارش انتخاب کن. تمام — از این لحظه یک «منِ محلی» داری که
   همهٔ قوانین این پروژه را از خود ریپو می‌خواند.

## دو کارِ اولی که به Cowork بده (کپی کن، بفرست)

**۱. تحویل دادهٔ تاریخی (دستور ۲۵ اوت):**
```
اول git pull کن. بعد از پوشهٔ claude-liam-signal/python این را اجرا کن:
python -m hamid.history_ingest --src "C:\AuraLiamData\research"
خروجی‌اش (brain/research/history/inventory.json) را commit و push کن.
```
این اسکریپت ۲۹۸ فایل کندل و متاها را بررسی می‌کند، شناسنامه‌شان را برای
اتاق تاریخچه بایگانی می‌کند و درِ دسترسی می‌سازد. خودِ ۵۶۷ مگابایت داده
سر جایش روی لپ‌تاپ می‌ماند (داخل گیت نمی‌رود — سنگین و بی‌دلیل است)؛
فقط شناسنامه push می‌شود تا من هم از این‌جا ببینم چه داریم.

**۲. روشن کردن حلقهٔ زندهٔ ۳۰ثانیه‌ای:** دوبار کلیک روی `live.command`
(مک) یا `live.bat` (ویندوز) — طبق `LIVE-LAPTOP-fa.md`. لپ‌تاپ خوابید،
گیت‌هاب خودش پشتیبان است و هیچ‌چیز نمی‌شکند.

## تقسیم کار نهایی (تا وقتی خودت عوضش نکنی)

- **لپ‌تاپ/Cowork**: حلقهٔ زندهٔ ۳۰ثانیه‌ای، کارهای روی دادهٔ محلی
  (مثل همین بایگانی)، و هر کاری که دسترسی به فایل‌های خودت می‌خواهد.
- **گیت‌هاب**: دفتر مشترک + پشتیبانِ ۲۴ساعته + تست‌های نگهبان + میزبانی
  پنل. (پیام‌های تکراری خرابی هم از دیشب درمان ریشه‌ای شده — اگر حتی یکی
  آمد، فورواردش کن.)
- **Artifact**: صفحه‌های دیدنی — گرافیفای و گزارش‌های بصری.
- **نشست ابری موازی (Dispatch)**: کارهای سنگین یک‌باره، مثل تحلیل عمیق
  همین دادهٔ تاریخی بعد از بایگانی.
