# محیط مشترک رانرهای GitHub Actions — یک فایل، نه یازده خطِ pip پراکنده.
# ۲ سپتامبر: پاسبان گزارش کار روی رانر افتاد چون pyyaml فقط در سه ورک‌فلو
# نصب می‌شد. هر ورک‌فلو: `pip -q install -r requirements-ci.txt`.
pyyaml
matplotlib
anthropic
# امضای فارسی روی عکس چارت (دستور ۳ سپتامبر). matplotlib خودش نه شکل‌های
# چسبانِ فارسی می‌سازد و نه راست‌به‌چپ می‌چیند؛ بی‌این دو، «پنل ققنوس و
# محافظانش» روی عکس حروفِ جدا و برعکس چاپ می‌شود.
arabic-reshaper
python-bidi
