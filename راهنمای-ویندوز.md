# لیام تریدر ۹ روی ویندوز — راهنمای حمید

هیچ فرمانی تایپ نمی‌کنی. فقط دوبار کلیک.

---

## بار اول (یک ربع، یک بار برای همیشه)

### گام ۱ — پایتون را نصب کن

اگر از قبل داری، از این گام رد شو.

۱. کلید ویندوز را بزن، بنویس `powershell`، Enter.
۲. این خط را کپی کن، داخل پنجرهٔ آبی راست‌کلیک کن (پیست می‌شود)، Enter:

```
winget install -e --id Python.Python.3.12
```

۳. تمام که شد، همان پنجره را ببند.

> اگر `winget` نشناخت: برو به python.org، نسخهٔ ویندوز را دانلود کن، و
> موقع نصب حتماً تیکِ **Add python.exe to PATH** را بزن.

### گام ۲ — گیت را نصب کن (برای گرفتن نسخه‌های بعدی)

همان پنجرهٔ آبی، همین خط:

```
winget install -e --id Git.Git
```

> این اختیاری است. بدونش هم سرویس کار می‌کند، ولی نسخه‌های بعدی خودکار
> نمی‌آیند.

### گام ۳ — پوشهٔ پنل را روی لپ‌تاپ بیاور

> **PowerShell را ساده باز کن، نه با Run as administrator.** با
> ادمین، پنجره در `C:\WINDOWS\system32` باز می‌شود و ویندوز اجازهٔ
> نوشتن آن‌جا را نمی‌دهد — همان خطای
> `could not create work tree dir: Permission denied`.
>
> و از `Desktop` استفاده نکن: اگر OneDrive روشن باشد، دسکتاپ واقعی
> جای دیگری است و `cd $HOME\Desktop` خطای `does not exist` می‌دهد.

پس مسیر ساده و همیشه‌درست، `C:\liam9`. این دو خط:

```
git clone https://github.com/AuraLiam/Liam-Trader-9.git C:\liam9
cd C:\liam9
```

حالا پوشه‌ات `C:\liam9` است. در File Explorer، در نوار آدرس بالا
`C:\liam9` را بنویس و Enter بزن تا بازش کنی.

> اگر گیت نصب نکردی: برو به
> `https://github.com/AuraLiam/Liam-Trader-9` → دکمهٔ سبز **Code** →
> **Download ZIP** → فایل را در `C:\liam9` باز کن (Extract).

### گام ۴ — روشنش کن

داخل `C:\liam9`، روی **`LIAM9.cmd`** دوبار کلیک کن. (ساده دوبار کلیک،
نه Run as administrator.)

بار اول خودش این کارها را می‌کند و طول می‌کشد (چند دقیقه):

- محیط پایتون را می‌سازد
- کتابخانه‌ها را نصب می‌کند
- فایل توکن (`live.env`) را می‌سازد و در Notepad **باز می‌کند**

وقتی Notepad باز شد، دو خط آخرش را پر کن:

```
TELEGRAM_BOT_TOKEN=توکنِ ربات @LiamTrader9_Bot
TELEGRAM_CHAT_ID=شناسهٔ عددی چت خودت
```

`Ctrl+S` بزن و پنجرهٔ Notepad را ببند. سرویس خودش ادامه می‌دهد و
مرورگر با پنل باز می‌شود.

---

## هر روز

روی **`LIAM9.cmd`** دوبار کلیک. تمام.

خودش آخرین نسخه را می‌گیرد و روشن می‌شود. پنجره را باز بگذار — همان
پنجره سرویس است. بستنش یعنی خاموش‌کردن.

---

## سه فایل دیگر

| فایل | کارش |
|---|---|
| **`LIAM9-DOCTOR.cmd`** | فقط معاینه: می‌گوید چه چیزی آماده است و چه چیزی نه. چیزی را روشن نمی‌کند. |
| **`LIAM9-AUTOSTART.cmd`** | یک بار بزن؛ از آن به بعد با هر روشن‌شدن ویندوز، خودش بالا می‌آید. |
| **`لیام۹.cmd`** | همان `LIAM9.cmd` با اسم فارسی. |

---

## پنل کجاست؟

`http://127.0.0.1:9009` — خودش موقع روشن‌شدن باز می‌شود.

این آدرس فقط روی همین لپ‌تاپ کار می‌کند. عددهایی که می‌بینی همان
لحظه‌ای‌اند که سرویس نوشته — دیگر منتظر هیچ انتشاری نمی‌مانی.

---

## اگر مشکلی بود

**اول `LIAM9-DOCTOR.cmd` را بزن.** هر خطش یا ✓ است یا ✗ با دلیلِ فارسی.
همان صفحه را برایم بفرست؛ از رویش دقیقاً می‌فهمم چه چیزی لنگ است.

سه چیزی که ممکن است ✗ باشد و معنی‌شان:

| ✗ | یعنی | کار |
|---|---|---|
| توکن تلگرام | فایل `live.env` خالی است | بازش کن و توکن را بگذار |
| دسترسی به fapi.bitunix.com | اینترنت به صرافی نمی‌رسد | VPN/فیلترشکن را چک کن |
| کندل واقعی می‌آید | هیچ صرافی‌ای جواب نداد | همان بالا |

---

## چند چیز که باید بدانی

- **لپ‌تاپ نباید بخوابد.** تنظیمات ویندوز → Power → حالت خواب را روی
  Never بگذار، وگرنه سرویس با خواب لپ‌تاپ می‌ایستد.
- **سرویس اگر بمیرد، ده ثانیه بعد خودش برمی‌گردد.** لازم نیست کاری کنی.
- **توکن هرگز به گیت‌هاب نمی‌رود.** فایل `live.env` در فهرست نادیده‌گرفته
  است.
- **معامله‌ای انجام نمی‌شود.** این سرویس سیگنال می‌فرستد و تحلیل می‌کند؛
  سفارش زنده خاموش است (`LIVE_EXECUTION=false`) و روشن‌شدنش تأیید جدا و
  صریح خودت را می‌خواهد.
