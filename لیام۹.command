#!/usr/bin/env bash
# لیام تریدر ۹ — روی همین فایل دوبار کلیک کن (مک). همین.
# اولین بار محیط را می‌سازد (~۱ دقیقه)، بعدش مستقیم بالا می‌آید.
cd "$(dirname "$0")" || exit 1
exec bash service/liam9.sh "$@"
