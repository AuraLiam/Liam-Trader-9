# Working agreement

## Standing instruction — do not wait to be asked

On every task, use the available connectors and plugins according to what each
is actually good at. This is not something Hamid should have to repeat.

| | what it carries |
|---|---|
| **GitHub Actions** | All heavy compute. Paper-trading cycles, parameter searches, deploys. Never the laptop. |
| **Notion** | The comparable record. One row per cycle with expectancy, interval, verdict, and the change made. |
| **Google Drive** | Durable archive of reports, so numbers outlive any machine. |
| **Gmail** | Digests as drafts. Never send from Hamid's account without being asked. |
| **Google Calendar** | The cadence, made visible. |
| **Telegram** | Live delivery from the panel: signals, outcomes, room reports. |
| **n8n** | Orchestration between the above, so the pipeline runs without a session open. |
| **Canva** | Only when something genuinely needs to be a graphic. Inventing work for a tool is not using it. |

If a connector's token has expired, say so plainly and carry on with the rest.

## What must not change

The strategy, the rooms, the supervisor, the two-hourly review with **one
controlled change per cycle** graded on the cycle after, and the rule that a
finding is only acted on once its confidence interval clears zero. Connectors
carry the work; they do not decide anything.

## How results are reported

Every number comes from a measurement that can be re-run. `tests/` holds the
simulator, harness, cycle runner, parameter search, gate funnel and age split.
A number without a way to reproduce it does not get reported.

State plainly whether a figure comes from simulated markets or live candles.
The simulator has volatility clustering, fat tails and regime switching, and it
tests whether the engine has an edge against a hard model — not whether it
makes money on the real tape.

That distinction turned out to matter more than it sounded. Measured on
30 July 2026, the simulator said 45% win and +0.283R where 3,931 trades on real
candles said 22.7% and +0.069R. So a simulator number is never a performance
claim. It is only ever "this rule moved expectancy by X against the same
simulator". Performance claims come from `python/backtest.py`, on real klines.

## Corrections

When a previous conclusion turns out to be wrong, say so directly and show the
measurement that overturned it. Two have already been corrected this way: a
freshness window widened on the strength of a friendly tape, and a claim that
the second pullback carried no advantage.

## Repository layout

- `index.html` — the panel. One file, no build step.
- `sw.js` — service worker. Bump `CACHE` on every deploy.
- `tests/` — simulator, harness, and every measurement script.
- `claude-liam-signal/` — reference material, work plan, cycle reports.
- `claude-liam-signal/agent/` — the standalone Node agent. `engine.js` is
  generated from `index.html`; rerun `extract-engine.js` after engine changes.
- `claude-liam-signal/n8n/` — importable orchestration workflows.

Deploys go to `main` (source) and `gh-pages` (served panel), in this repo
(`Auraliam/Liam-Trader-9`).

## LIAM — ارکستراتور و زیرایجنت‌ها

قانون اساسی: `claude-liam-signal/LIAM-CHARTER.md` (verbatim — تغییر فقط با
دستور صریح حمید) · نگاشت وضعیت: `LIAM-STATUS.md` · قوانین عملیاتی:
`.claude/rules/trading-core.md` و `.claude/rules/ibs-pullback-plus.md`.

**ایجنت اصلی ارکستراتور است، نه همه‌کاره.** در هر رویداد مهم بپرس: چه
عوض شد؟ چرا مهم است؟ کدام فرضیه اثر می‌گیرد؟ چه اطلاعاتی کم است؟ کدام
متخصص سریع‌تر جوابش را دارد؟ ستاپ فعالی باطل می‌شود؟ فرصت تازه‌ای ساخته
می‌شود؟ تحقیق لازم است؟ چه چیزی باید به خاطر سپرده شود؟ — بعد به
زیرایجنت مناسب در `.claude/agents/` واگذار کن؛ هر ایجنت را فقط وقتی صدا
بزن که رویداد لازمش کرده، نه مکانیکی در هر چرخه:

| رویداد | زیرایجنت |
|---|---|
| «قیمت کجاست؟» / اعتبار ستاپ | market-structure |
| قبل از سیگنال اصلی، ویک بزرگ | liquidity |
| حرکت ناگهانی BTC، سیگنال پراطمینان | macro-dominance |
| پامپ/دامپ معنادار، همبستگی غیرمنتظره | lead-lag |
| تسویه، استاپ، شکست تکرارشونده | post-trade-learning |
| ۳+ شکستِ همان الگو، رفتار بی‌توضیح | research (فقط پرسش-محور) |
| تأیید لحظهٔ ورود | execution |
| اعتبار اردر بلاک قبل از ستاپ، استاپ روی سطح، بازپایش OBها، رویداد OB_APPROACHING/BREAKER_DETECTED از `signals/ob-radar.json` | order-block |
| دادهٔ مشکوک/ناسازگار | data-quality |

**قانون پیش‌فرض هر انجین/ایجنت جدید (دستور حمید، ۱۳ اوت)**: یادگیری
دائمی از روز اول فعال است — خروجی‌هایش روی پروندهٔ معامله ثبت می‌شود،
ماشین بونفرونی شبانه سهمش را از نتیجه می‌سنجد، و درس‌هایش با memory به
حافظهٔ دائمی می‌رود. انجینی که ردپای قابل‌سنجش نگذارد، ناقص تحویل شده.

**حافظه**: قبل از تحلیل مهم بخوان، بعد از نتیجهٔ معنادار بنویس — فقط دانش
قابل‌استفادهٔ مجدد (`brain/memory/README.md`؛ چهار لایهٔ جدا: قوانین هسته
/ یادگیری تجربی / فرضیهٔ آزمایشی / تحقیق). پرونده‌های معامله در
`brain/cases/` (فایل یکتا، append-only). تحقیق در `brain/research/`
(ایزوله از تولید؛ چرخهٔ اجباری در README همان پوشه).

**ایمنی وضعیت مشترک**: دو ورک‌فلو هم‌زمان روی یک فایل ننویسند — الگوهای
موجود: reapply ضدتصادم، فایل یکتای case، اجتماع (union) برای لاگ/درس/
تاریخچه، concurrency group در ورک‌فلوها. وضعیت تولیدشدهٔ runtime
(signals/, brain/) از دانش سورس‌کنترل‌شده (rules/, python/) جداست.

## بستهٔ مهارت‌های v2.1 (نصب ۱۴ اوت — سند حمید)

بستهٔ `AuraLiam369_Claude_Fable5_Skills_Package_v2_1` در ریشه نصب شده است.
اسناد مرجعش: `AuraLiam369_Trading_Agent_Master_Spec_FA_v2.1.md` ·
`.claude/rules/00..06-*.md` · `config/engine_registry.yaml` · `docs/*_FA.md`.
پرامپت اجرا: `prompts/CLAUDE_FABLE5_APPLY_NOW_FA.txt` (مرحله ۰ = فقط Audit).

- **مرز معماری**: کلود کد سازنده/ممیز است، نه اسکنر ۲۴/۷. حلقهٔ زندهٔ
  هدفِ v2.1 با سرویس Python اجرا می‌شود؛ GitHub Actions به CI/بک‌فیل/گزارش
  برمی‌گردد. تا وقتی آن سرویس مستقر نشده، چرخهٔ فعلی Actions همان مرجع
  عملیاتی است — هیچ‌چیزِ کارکرده قبل از جایگزینِ اثبات‌شده خاموش نمی‌شود.
- **دو خانوادهٔ زیرایجنت**: ۹ ایجنت دامنهٔ رویدادمحور بالا (عملیاتی) سر
  جایشان‌اند؛ ۲۷ ایجنت `e00..e26` متخصص read-only ممیزی/ساخت‌اند که
  فقط `ENGINE_REVIEW_PACKET` برمی‌گردانند و هیچ فایل مشترکی را ویرایش
  نمی‌کنند — نوشتن فقط با ایجنت اصلی (سریالی).
- **قوانین جدید ۰۰–۰۶ مکمل `trading-core.md`اند نه جایگزین**؛ اگر جایی
  اختلاف بود، منشور LIAM و دستور صریح حمید حاکم است.
- **LIVE_EXECUTION=false** می‌ماند؛ فقط Backtest/Replay/Paper/Live-Signal.
- بعد از هر تغییر: `python scripts/validate_skill_package.py` (هوک Stop
  هم خودش اجرایش می‌کند).

## قانون ضد-merge (دستور حمید، ۲۶ اوت شب — «آقا merge نکن دیگه»)

«همه رو کنار هم نگه‌دار و شماره‌گذاری کن… دیگه فایل‌ها و اطلاعات رو
merge نکن.» از این شب:

1. **تغییر کد = کامیت کوچک مستقیم روی سرِ main** (بعد از تست سبز)، نه
   شاخه+merge. بک‌آپِ تأییدشده روی ریموت قبل از شروعِ هر دستهٔ تغییر
   همچنان اجباری است (`backup/main-pre-<موضوع>-<تاریخ>`).
2. **دادهٔ runtime هرگز بازنویسی/ادغامِ متنی نمی‌شود.** یا append-only
   شماره‌دار است (`signals/archive/*.jsonl`، ردیف‌های `n` پیاپی)، یا
   اجتماع بر «هویت» ردیف فقط برای جلوگیری از گم‌شدن (trade_key /
   کلید+تازه‌ترین مهر) — هیچ فایل دفتری با نسخهٔ دیگر «یکی» نمی‌شود.
3. **ساختار اصلی پنل (index.html) و اطلاعات قبلی دست نمی‌خورد** مگر
   دستور صریح؛ هر تغییر پنل باید diff کوچکِ قابل‌بازبینی باشد.

ریشهٔ دستور: از شروع مرج‌های پیاپی، وضعیت‌های runtime (دفتر ضدتکرار،
دفتر پیپر) در تصادم‌ها گم می‌شد — نمونهٔ اندازه‌گیری‌شده: PAXG×۵ در ۲۱
دقیقه (۲۶ اوت). رفع در `telegram.py` (سه‌منبعی) + `pump_radar.reapply`
(بقای دفترها) + محافظ `hamid/test_telegram_dedupe.py`.

## قانون بک‌اپ قبل از merge (دستور حمید، ۲۴ اوت)

«قبل از مرج همیشه یه بک‌اپ بگیر که گند نزنی.» — قبل از هر merge به
`main`، اول یک برنچ بک‌اپ از سرِ فعلی main ساخته می‌شود
(`backup/main-pre-<موضوع>-<تاریخ>`) و وجودش **روی ریموت راستی‌آزمایی
می‌شود** (list از API، نه اعتماد به پیام push — «Everything up-to-date»
یک بار بعد از قطعی شبکه دروغ گفت). بدون بک‌اپِ تأییدشده merge نمی‌شود.
برگشت: `git checkout -B main backup/... && push`. بک‌اپ‌های کهنه بعد از
یک هفتهٔ بی‌حادثه پاک می‌شوند.

## انتقال مهارت به داشبورد — قفسهٔ لبه (دستور حمید، ۲۴ اوت)

«از بهترین نتایج یک کد پایتون بساز و برو سمت تأثیرگذاری و انتقال مهارت
از این‌جا به داشبورد.» — `hamid/edge_export.py` هر چرخه قانون‌های
**CI-گذشتهٔ** بک‌تست شبانه را (همان فیلتر `scan.confirmed_rules`) با
شناسنامهٔ کامل (n، CI، دلتا، تاریخ) در `signals/edge.json` می‌گذارد؛
داشبورد (liam9_strategy v2.8) با `sync_all` می‌کشد و همان دلتاها را روی
امتیاز می‌گذارد — سقف ±۱۵، وتو نه، قفسهٔ کهنه‌تر از ۴۸ساعت بی‌اثر،
ردپای `edge_used` روی هر خروجی برای سنجش شبانه. چیزی که CI رد نکرده
هرگز صادر نمی‌شود — قفسهٔ خالی از قفسهٔ آلوده بهتر است.
محافظ: `hamid/test_edge_export.py` (۲۳ بررسی).

## دستور شب ۲۶ اوت — یادگیری بی‌وقفه و استدلال روشن (حمید، قبل از خواب)

1. **هر ترید پیپر = یک درس.** هیچ معاملهٔ پیپری بدون هضم (پرونده/درس)
   بسته نمی‌شود — از قبل قانون ۰۳ است؛ این‌جا تأکید مجدد صاحب پنل است.
2. **آزادی فقط در پیپر**: ایجنت‌ها در پیپرمود اجازهٔ آزمایش تایم‌فریم و
   تحلیل کندلی متفاوت دارند تا تجربه بسازند — دفتر جدا (experiments)،
   هرگز روی سیگنال واقعی تا CI رد نشده (مرز قانون ۰۳/۱۲ پابرجا).
3. **Reasoning فعال**: هر ایجنت استدلالی قبل از هر حکم، زنجیرهٔ دلیلش را
   صریح می‌نویسد (شواهد → تفسیر → حکم → ابطال‌کننده) و اگر چیزی را
   نمی‌فهمد اول از منبع معتبر جست‌وجو می‌کند، حدس نمی‌زند.
4. **معماری هم آزمایش‌پذیر است**: جای انجین‌ها/ترتیب دروازه‌ها در
   بک‌تست قابل‌آزمایش است — چند روش بک‌تست موازی، فقط با داوری CI.
5. **منابع بیرونی**: تحلیل تحلیلگران برتر (TradingView و مشابه) به‌عنوان
   «شاهد با ~۳۰٪ خطا» دیده می‌شود — گوشهٔ ذهن، نه دروازه؛ ورودش به
   تصمیم فقط از مسیر قانون ۰۳. دسترسی X (توییتر) هنوز برقرار نیست.
6. **یادآوری اجرا**: بعد از دیدن نتایج، مرتب به حمید یادآوری شود که
   کارهای تأییدشده را با هم اجرایی کنیم — نتیجهٔ بی‌اجرا کار نیمه‌کاره
   است.

## قانون سه‌بار خواندن (دستور حمید، ۲۳ اوت)

هر پیام حمید سه بار خوانده می‌شود: بار اول برای فهم، بار دوم برای استخراج
تک‌تک خواسته‌ها به‌صورت چک‌لیست، و بار سوم **در پایان کار** برای بررسی
این‌که همهٔ خواسته‌ها واقعاً انجام شده — در ریپو، نه در وعدهٔ پاسخ.
خواسته‌ای که انجام‌شدنی نیست با دلیلش صریح گفته می‌شود، نه بی‌صدا حذف.
پاسخ نهایی آینهٔ همان چک‌لیست است. (نقض این قانون یک بار در ۲۳ اوت
اتفاق افتاد — منابع اعلامی به ایجنت‌ها تزریق نشده بود.)

## قانون خواندن پیش از پاسخ (دستور حمید، ۱۶ اوت)

قبل از هر پاسخ یا شروع هر کار، اول گفتگوهای قبلی خوانده می‌شود — از جمله
دو صفحهٔ چتِ پین‌شده و نتیجه‌گیری‌های انتهای هر بحث. جواب باید بر پایهٔ
آخرین نتیجه‌گیری باشد، نه حافظهٔ مبهم از وسط بحث. تکرار حرف قبلاً
گفته‌شده، پرسیدن چیزی که قبلاً جواب داده شده، یا قاطی کردن دو موضوع =
نشانهٔ نخواندن، و نقض همین قانون. اگر چیزی در چت با وضعیت فعلی کد
نمی‌خواند، اول همان اختلاف گزارش می‌شود.

## لیام تریدر ۹ — هویت و جدایی (دستور حمید، ۱۶ اوت)

- این چت/پنل از این لحظه **کاملاً جدا از چت AuraLiam Max** است؛
  نتیجه‌گیری‌های آن چت این‌جا مرجع نیستند و برعکس.
- نام پنل و **برند همهٔ سیگنال‌های ارسالی: «لیام تریدر ۹»**
  (`telegram.PANEL_NAME` مرکزی است — تغییر برند فقط از همان‌جا).
- کتابخانهٔ دائمی ایجنت‌ها: `brain/library/` — ورود مطلب فقط پس از
  راستی‌آزمایی (اسکیمای README همان پوشه)؛ حلقهٔ شبانه از قفسه می‌خواند
  و برداشت هر انجین در `brain/research/` ثبت می‌شود.

## آستانهٔ پول واقعی — بیت‌یونیکس (دستور حمید، ۱۶ اوت)

**LIVE_EXECUTION همچنان false** — فعال‌سازی فقط با تأیید صریح و جداگانهٔ
حمید، و فقط بعد از برآورده شدن پیش‌نیازهای زیر (از کتابخانهٔ درس‌های
ربات‌تریدری، `brain/library/`، همه VERIFIED):

1. **دروازهٔ کارمزد پیش از هر پوزیشن** — `hamid/fees.py`: RR باید خالص از
   کارمزد+لغزش از حد بگذرد. اعداد راستی‌آزمایی‌شده (۱۶ اوت): Bitunix
   فیوچرز VIP0 میکر ۰.۰۲٪ / تیکر ۰.۰۶٪؛ تیکر دو سر + لغزش = ~۰.۱۵٪ —
   منطبق با قانون تریل. میکر بودن مزیت است؛ استاپ تنگ سهم کارمزد از R را
   چند برابر می‌کند (دام اسکالپ).
2. **ارزهای صفر-کارمزد**: لیست فقط از منبع رسمی صرافی (وضعیت فعلی:
   UNVERIFIED در `config/fees.json`)؛ بعد از تأیید، اولویت تحلیل با
   آن‌هاست. ارزهای وابسته به ساعت بازار امریکا جدا مدیریت می‌شوند.
3. **کیل‌سوییچ چندماشه** قبل از لایو: سقف ضرر تجمعی + نرخ غیرعادی سفارش +
   انحراف قیمت + dead-man's switch؛ فعال‌شدن = لغو سفارش‌ها، بستن
   پوزیشن‌ها، آلارم ثانیه‌ای.
4. **سقف سخت سایز**: استراتژی حق تعیین سایز بی‌سقف ندارد؛ ۱–۵٪ بر ترید،
   سقف اکسپوژر کل، سایز معکوس نوسان.
5. **گذار مرحله‌ای**: عدد پیپر سقف خوش‌بینانه است (فیل کامل/بی‌لغزش)؛
   لایو فقط بعد از دورهٔ پایدار پیپر با همین دروازه‌ها.
6. **Reconciliation**: در فاز لایو، پوزیشن‌های واقعی صرافی دوره‌ای با
   دفترها تطبیق داده می‌شوند؛ بی‌خبری = رفتار امن، نه فرضِ فیل.

## امضای پنل روی هر پیام (دستور حمید، ۱۶ اوت)

دو پنل جدا (لیام تریدر ۹ / AuraLiam Max) = دو استراتژی جدا. هر پیام
تلگرام — سیگنال، رسید، آلارم — باید امضای پنل فرستنده را داشته باشد تا
حمید بداند از کدام پنل آمده. این‌جا: `telegram.PANEL_NAME` («لیام تریدر
۹»). رسید TradingView برند را از فیلد `panel` خود آلارم می‌گیرد؛ آلارم
بی‌امضا صریح «بدون امضای پنل» می‌خورد — امضا حدس زده نمی‌شود. در JSON
آلارم‌های TradingView این پنل همیشه `"panel":"لیام تریدر ۹"` بگذار.

## قانون رفع قطعی (دستور حمید، ۱۷ اوت)

حمید گزارش «یک مشکل پیدا شد» نمی‌خواهد؛ سیستمِ درست‌کار می‌خواهد.
از این پس:

1. هیچ مشکلی «گزارش» نمی‌شود مگر همراه با: رفعِ ریشه‌ای + محافظ دائمی
   (تست/پاسبان) که برگشتش را ناممکن کند + اثبات اجراشده. مشکلِ بسته فقط
   یک خط در گزارش است، نه تیتر.
2. مشکل تکراری از یک کلاس = شکست محافظ قبلی؛ اول محافظ تعمیر می‌شود.
3. کد جدید بدون تستِ همراه تحویل نمی‌شود — عیبِ کدِ تازه باید در همان
   نشستِ ساختنش بمیرد، نه در بازرسی بعدی.
4. گزارش‌ها با «چه چیزی کار می‌کند و چه اعدادی داد» شروع می‌شوند؛
   خرابی‌های بسته‌شده در انتها، یک‌خطی.

## دروازهٔ روند در گلوگاه ارسال (دستور حمید، ۱۷ اوت)

مشاهدهٔ حمید: «چارت کاملاً صعودی ولی سیگنال شورت صادر شده و برعکس.»
از این پس در گلوگاه ارسال (telegram.send_signals) و شلیک دفتر انتظار:

- **هر دو تایم بالا (۴س و ۱س) خلاف جهت سیگنال → وتوی مطلق.** هیچ امتیاز
  و استثنایی عبور نمی‌دهد (اجرای سختِ قانون ۲).
- **یک تایم بالا خلاف → «خلاف روند»**: فقط با تمام تأییدیه‌ها می‌گذرد
  (inOB + سوییپ + CHoCH + FVG + کیفیت ≥۷۰) و پیام برچسب «⚠️ خلاف روند»
  با فهرست تأییدیه‌ها می‌گیرد؛ یک غایب = NO_SIGNAL.
- دادهٔ روند ناموجود = NO_SIGNAL (قانون ۱)، نه عبور کور.
- فرستنده trend4/trend1 را روی لاگ می‌نویسد؛ پاسبان C6 هر ارسالِ
  دورزننده را تخلف high می‌گیرد.
- پیاده‌سازی: hamid/trend_gate.py · محافظ: test_trend_gate (۷ آزمون) +
  C6 پاسبان.

## استقلال کامل (مهاجرت ۱۷ اوت)

این ریپو خانهٔ انحصاری «لیام تریدر ۹» است. ریپوی قدیمی (.sognal) متعلق
به پنل دیگر است — از این پس هیچ خواندن/نوشتنی آن‌جا انجام نمی‌شود و
نتیجه‌گیری‌هایش مرجع نیست. برند، دفترها، حافظه و سیگنال‌ها همه از صفرِ
همین‌جا.

## شب اجرا — قوانین حمید (۱۷ اوت، شب)

1. **جایزهٔ انجین‌ها**: هر انجینی که ردپای تأییدش روی معاملهٔ TP باشد
   جایزه می‌گیرد (TARGET=+۳، TRAIL=+۱، STOP=−۱)؛ **TP از تجربه (exp_used)
   ۲×**. دفتر: `hamid/rewards.py` → `brain/rewards.json` +
   `signals/rewards.json`. جایزه انگیزشی/عیب‌یابانه است، وتو/وزن ندارد.
2. **ناظر پیش‌بینی دامیننس**: هر نوبتِ اتاق دامیننس (زنجیرهٔ رادار،
   گام مؤثر ~۳-۵ دقیقه) پیش‌بینی مسیر USDT.D/BTC.D با **دلایل اجباری**
   ثبت و پیش‌بینی‌های سررسیده با واقعیت نمره می‌خورند؛ کارنامه روی
   `signals/dominance.json`. تأیید دروازه‌ای: ≥۲ شاهد هم‌جهت + دادهٔ تازه؛
   غیر آن NO_FORECAST. تحلیل مولتی‌تایم: **اول ۴س، بعد ۱س** — سطوح +
   خط معتبر ≥۳ برخورد (`dominance.multi_tf`).
3. **ایجنت ۲۷ — ناظر کل (E26)**: هد-تریدر سخت‌گیر مجموعه؛ هر چرخه از
   کارنامه‌های ثبت‌شده دستور تمرکز مستدل صادر می‌کند
   (`hamid/overseer.py` → `signals/overseer.json`). بدون وتو؛ کتابخانه‌اش
   مدیریت/روانشناسی/اقتصاد + منابع خبر (ainvest، coinmarketcap،
   coingecko، coindesk، theblock).
4. **آزمایش فیبوناچی روی IBS+پولبک**: عمق پولبک (`premortem.fib_ratio`)
   روی هر سیگنال ثبت و با دو شرط بونفرونی («ناحیهٔ طلایی ۰.۵–۰.۷۰۵» و
   «کم‌عمق <۰.۳۸») شبانه داوری می‌شود — قانون فقط با CI بالای صفر.
5. **حلقهٔ مطالعهٔ ۳ساعته**: `research/reading_loop.py` (ورک‌فلوی
   reading-loop) هر ۳ ساعت یک کتاب از برنامهٔ درسی یک انجین (چرخشی)
   توصیه و در `brain/research/<Exx>/reading.jsonl` ثبت می‌کند؛ کتاب فقط
   بعد از راستی‌آزمایی وارد قفسه می‌شود (`brain/library/queue.jsonl`).
6. **پایش ۲۰۰ ارز**: live-scan هر ۱۵ دقیقه (دقایق ۷/۲۲/۳۷/۵۲) ۲۰۰ جفت
   برتر را با پایتون قطعی می‌گردد؛ رادار پامپ/دامیننس با گام مؤثر ~۳-۵
   دقیقه. سقف زمان‌بند Actions پنج دقیقه است — پایش ۳دقیقه‌ای واقعی کار
   سرویس محلی Python است (قانون ۰۲)، نه بهانهٔ توقف Actions.

## قانون شوک بیت‌کوین و خط زندهٔ امن (دستور حمید، ۱۹ اوت)

**۱. شوک روی هر تایم‌فریم.** وقتی بیت‌کوین روی هر تایمی (۱د/۵د/۱۵د/۱س/۴س)
یک‌باره پامپ یا دامپ کرد، موتور بر اساس استراتژی و **اردر بلاکِ همان
ایمپالس** با **اهرم ۵ تا ۶** معامله می‌کند. «یک‌باره» تعریف عددی دارد:
بدنه ≥ ۲.۵×ATR همان تایم **و** بزرگی حرکت ≥ کف مطلق آن تایم (۰.۳۵٪ در ۱د
تا ۲.۵٪ در ۴س) **و** حجم ≥ ۲× میانهٔ ۵۰ کندل. ورود وسط حرکت ممنوع است —
فقط بازگشت به OBِ ایمپالس، با شرط جابه‌جایی و مصرف‌نشدگی.

**۲. شکار پامپ با اهرم ۱۵ — فقط با تأیید ۱۰۰٪ حجم.** شش شرط حجمی
(حجم ≥۳× میانه · حجم صعودی در ایمپالس · کلوز در ۲۰٪ انتهای دامنه · ویک
مخالف <۳۰٪ · حجم کندل بعدی نریخته · جریان حجم رو به بالا). **یکی غایب =
اهرم ۱۵ ممنوع**؛ موتور یا به حالت ۵–۶ برمی‌گردد یا NO_SIGNAL. «۱۰۰ درصد»
یعنی صد درصد، نه پنج از شش.

**۳. اهرم هرگز از محافظ لیکویید و سقف داشبورد رد نمی‌شود.** اهرم ≤
min(خواسته، ۵۰÷استاپ٪، سقف داشبورد=۲۰). سایز از قانون ریسک ۲٪ می‌آید نه از
اهرم: ضررِ استاپ همیشه ۲٪ می‌ماند و اهرم فقط مارجین را کم می‌کند.

**۴. شوک بیت‌کوین به‌تنهایی مجوز ورود آلت نیست** — نماد باید خودش همراهی
کرده باشد (قانون ۳: بستر اجباری است، نه جایگزین ساختار خود نماد).

**۵. خط ارتباط زندهٔ امن** (`liam9_link.py`): دو جهت، هر دو با
HMAC-SHA256 و کلید فقط از محیط (`LIAM9_LINK_SECRET`).
- بالا-رو: هر ضربان/تصمیم/ردشدن با دلیل در `signals/live-link.json`
  (حلقهٔ ۲۰۰ رویداد) — یعنی لحظه‌ای معلوم است موتور چه دید و چرا نرفت.
- پایین-رو: `signals/link-commands.json` — فرمان امضاشده با `seq` صعودی و
  انقضا. فهرست سفید شش‌تایی (set_param محدود به جدول بازه‌ها، set_risk،
  pause، resume، hint، watch). ضدبازپخش، سقف اندازه، و **رد همیشگی**
  هر فرمانی که بوی اجرای زنده بدهد. نبودِ کلید = رد همهٔ فرمان‌ها (نه
  قبول همه). اهرم، سقف اهرم و محافظ لیکویید از راه دور قابل تغییر نیستند.

**وضعیت (به‌روزشده ۲۴ اوت): ⏹ خاموش — دستور صریح حمید.**

شرطِ خودِ همین قانون این بود: «تا وقتی بازهٔ اطمینانش از صفر رد نشود،
فقط پیپر». بعد از **۳٬۱۶۳ معامله** رد نکرد:

| | مقدار | CI95 |
|---|---|---|
| ناخالص | **+۰.۰۸۲R** | [+۰.۰۴۸, +۰.۱۱۶] — بالای صفر |
| خالص از کارمزد | **−۰.۰۳۱R** | [−۰.۰۶۵, +۰.۰۰۵] — شاملِ صفر |
| جمع خالص | **−۹۹.۰R** | |

**فرضیهٔ شوک باطل نشد؛ هندسه‌اش شکست.** لبهٔ ناخالص واقعی است و CI
بالای صفر دارد — کارمزد تمامش را می‌خورد، چون استاپ تنگ است و
`کارمزد٪ ÷ استاپ٪` بزرگ می‌شود. همان بیماریِ اسکلپ ۱ دقیقه. پس
روشن‌کردنِ دوباره فقط با **هندسهٔ بزرگ‌تر** معنا دارد، نه با همین
پارامترها؛ و اهرمِ بیشتر جوابش نیست (اهرم در آن کسر اصلاً نیست).

کرونِ `shock-desk.yml` برداشته شد؛ کد و `workflow_dispatch` و
`test_shock.py` (۴۶ آزمون) دست‌نخورده ماندند تا بازآزمایی ممکن باشد.
محافظِ چسبندگی: `hamid/test_workflows.py` — برگشتِ کرون یا پاک‌شدنِ
دلیلِ عددی از روی فایل، چرخه را سرخ می‌کند (اثبات منفی گرفته شد).

پیاده‌سازی: `liam9_shock.py` · `liam9_link.py` · `hamid/shock_desk.py`.

## تک‌بات و تک‌گیت‌هاب — مرز قرمز (دستور صریح حمید، ۲۰ اوت)

**۱. سیگنال فقط روی یک بات: `@LiamTrader9_Bot`.** هر مقصد دوم/سوم ممنوع
است. آنچه ۲۰ اوت پاک شد: آینهٔ `TELEGRAM_*_2` در `telegram.py` و ۱۲
ورک‌فلو، بات جداگانهٔ اسکنر محلی (`LOCAL_TG_*`)، بات ایجنت نودی
(`TG_TOKEN/TG_CHAT`)، ورک‌فلوی `telegram-both.yml` و آزمون `test_mirror`.
اعتبارنامه فقط `TELEGRAM_BOT_TOKEN` و `TELEGRAM_CHAT_ID` — و بس.

**۲. فقط گیت‌هاب `Auraliam`.** هیچ ارجاع، لینک یا اتصال به هیچ مالک دیگری
در کد زنده. ریموت تنها `Auraliam/liam-trader-9` است.

**محافظ**: `hamid/test_single_bot.py` (۵ آزمون) — در دروازهٔ خودآزمایی
`hamid-cycle` و `pump-radar` اجرا می‌شود. هر متغیر بات دوم، هر منطق آینه،
یا هر ارجاع به مالک دیگری = چرخه سرخ می‌شود و سیگنال صادر نمی‌شود.

## بستهٔ دانش سیگنال-اول + قرارداد اجرا (دستور حمید، ۲۰ اوت)

**نصب بستهٔ ۱۹ اوت، اصلاح‌شده برای همین ریپو** (نسخهٔ خام برای ریپوی
قدیمی ساخته شده بود و دو فایل نصابش دور انداخته شد): قوانین
`.claude/rules/07..09-*.md` · مهارت‌های `liam-e10-order-flow-level2`،
`liam-e09-candlestick-evidence`، `liam-e12-pump-review`،
`liam-e23-signal-health` · `config/liam9_signal_priority_v1.yaml`
(**تایم تهران**) و `liam9_level2_features_v1.yaml` · `schemas/` (۴ قرارداد
JSON) · `evals/` (امتحان ≥۸۵٪ + پروتکل بک‌تست + دروازه‌های پذیرش) ·
`brain/library/curricula/` + ۲۴ منبع QUEUED در صف کتابخانه.

- **مالکیت‌ها (داوری ۲۰ اوت)**: E10 مالک Order Flow + Level 2؛ E08 فقط
  مصرف‌کنندهٔ پکت ALLOW/WAIT/REJECT با انقضا؛ E09 مالک هندسهٔ کمی کندل.
  حکم نهایی مالکیت L2 با بک‌تست رویدادی (برنامه در
  `brain/research/E10/findings.jsonl`)؛ با کندلِ تنها قابل‌سنجش نیست،
  پس `pre_move_anomaly` تا راه‌افتادن دادهٔ عمق **خاموش** است.
- **پامپ: دقیقاً ۵ نوبت در روزِ تهران** (`pump-review.yml`؛ کرون UTC
  `13 2,7,12,17,21`). بند «رادار پامپ با گام ~۳-۵ دقیقه» از قانون ۱۷ اوت
  **نسخ شد** — پشتوانه: دفتر آلارم n=۳۰۹۶، −۰.۱۸۰R، CI زیر صفر.
  زنجیرهٔ پیوسته (فایل `pump-radar.yml`، نام «Signal chain») فقط حامل
  اسکن استراتژی + دامیننس + الگوهای BTC است. محافظ:
  `hamid/test_pump_budget.py` (۱۳ بررسی).

**قرارداد اجرای داشبورد (دستور صریح، ۲۰ اوت)** — ریشه: پوزیشن بی‌استاپ/
بی‌تارگت، مارجین کراس، و شورت ARB در بازار مثبت:

1. هر پوزیشن **فقط ایزوله** — کراس ممنوع؛ خروجی هر سه فایل داشبوردی
   (`liam9_strategy` · `liam9_h1_strategy` · `liam9_shock_strategy`)
   `margin_mode=isolated` دارد و `liam9_link.validate_exec` سفارش کراس
   یا بی‌حالت-مارجین را رد می‌کند.
2. **استاپ و تارگت اجباری** — روی خود خروجی (`stop_loss`/`take_profit`)
   و در خط اجرا (`tp1` عددی مثبت وگرنه رد). سیگنال بی‌استاپ/تارگت باطل.
3. **دروازهٔ جهت بازار داخل خود فایل داشبورد**: آلت بدون کندل‌های BTC
   (۴س+۱س) = NO_SIGNAL (قانون ۳)؛ هر دو تایم BTC خلاف جهت = وتوی مطلق؛
   یک تایم خلاف = فقط با تأیید کامل (کندل هم‌جهت + کیفیت ≥۷۰) با برچسب
   «⚠️ خلاف بازار». استثنای صریح: اسکلپ ۱ دقیقه. بک‌تست H1 هم بستر BTC
   را بدون نگاه به آینده پاس می‌دهد.
4. **دروازهٔ روند روی مراحل منتشرشونده هم**: پروندهٔ ARB نشان داد داشبورد
   روی ستاپ ARMEDِ `signals/latest.json` هم عمل می‌کند؛ حالا
   `scan.gate_stages` هر SIGNAL/ARMED/PULLBACK_1 را قبل از انتشار از
   همان دروازهٔ روند رد می‌کند — وتو/تأیید ناقص/دادهٔ ناموجود → تنزل به
   WATCH با دلیل.
   محافظ: `hamid/test_exec_contract.py` (۱۵ بررسی؛ در hamid-cycle و
   shock-desk اجرا می‌شود).

## طرح معماری هدف + بستهٔ شواهد (دستور حمید، ۲۷ اوت)

- **سند ۸۲صفحه‌ای حمید = هدف رسمی معماری.** نگاشت زنده:
  `claude-liam-signal/ARCHITECTURE-BLUEPRINT.md` (وضعیت ۲۴ ساخته / ۵
  جزئی / ۵ در برنامه + ترتیب اجرای پنج شکاف). هیچ چیزِ کارکرده خاموش
  نمی‌شود؛ هر شکاف فقط از مسیر قانون ۰۳.
- **قانون ۱۲ — بستهٔ شواهد** (`.claude/rules/12-evidence-packet.md` ·
  `hamid/evidence_packet.py`): مدل رسمی پاسخ هر انجین = ادعا + اعداد +
  کارنامه + سناریوی دو جهت + باطل‌کننده + منابع + مرز صادقانه. حمید:
  «دقیقاً همین مدل پاسخ دادن را در کل ساختار می‌خوام.» ریسکِ بادلیل =
  ریسکِ سنجش‌پذیر؛ هیچ دروازه‌ای بی‌CI شل نمی‌شود.
- تقویم کلان: رأی دامیننس تا ۳ رویداد پیش رو (نه فقط نزدیک‌ترین)؛
  گزارش ساعتی دامیننس تقویم کامل با ساعت تهران + منابع + دوتکه‌شدن
  کپشن بلند. آنلاک توکن: `intel.unlocks` (شکل ناشناس = UNVERIFIED).
  محافظ: `hamid/test_evidence_packet.py` (۲۰) + `test_dom_report` (۱۲).
