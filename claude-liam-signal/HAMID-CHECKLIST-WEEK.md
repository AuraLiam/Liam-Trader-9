# چک‌لیست خواسته‌های حمید — هفتهٔ ۲۰ تا ۲۷ اوت ۲۰۲۶

دستور ۲۶ اوت شب: «تمام حرفای یک هفته اخیرم رو مو به مو بخونی و خواسته‌ها
رو در یک لیست قرار بدی و دونه به دونه انجامش بدی و کنارش تیک سبز بزنی و
ثابت کنی که انجامش دادی.» این همان لیست است. هر بند: خواسته → وضعیت →
اثباتِ در ریپو (نه وعده). ✅ = انجام و محافظ‌دار · ⏳ = در جریان با دلیل ·
⏹ = خاموش به دستور خود حمید یا حکم CI.

## شب ۲۶ اوت (همین امشب)

| # | خواسته | وضعیت | اثبات |
|---|---|---|---|
| ۱ | سیگنال تکراری نیاید (PAXG چند بار آمد) | ✅ | ریشه: حافظهٔ ضدتکرار فقط با pushِ گیت دوام می‌آورد؛ ۲۰:۱۲→۲۱:۰۱ هیچ push ننشست و ۵ بار رفت. رفع سه‌لایه در `telegram.py` (حافظهٔ کناری /tmp + ذخیرهٔ فوری بعد از هر ارسال + بازسازی از لاگ) و بقای دفترها در `pump_radar.reapply`. محافظ: `hamid/test_telegram_dedupe.py` (۱۸ بررسی، در دروازهٔ هر دو زنجیره) |
| ۲ | نتیجهٔ استاپ/انقضا ریپلای همان پیام سیگنال باشد | ✅ | مسیر ریپلای از قبل بود (cycle.py: تارگت/استاپ/تریل/منقضی با reply_to_message_id + چارت)؛ عیب این بود که ردیفِ دفترِ همان سیگنال با شکستِ push گم می‌شد و نتیجه‌ای وجود نداشت — همان رفع بند ۱ این را هم بست (open/closed.jsonl حالا از reset جان به در می‌برند) |
| ۳ | ارسال سیگنال فقط تایم ۱۵د و ۵د | ✅ | `telegram.ALLOWED_TFS={"5m","15m"}` در گلوگاه ارسال؛ tf دیگر بلند رد می‌شود. آزمونِ همین شب: سیگنال 1h/4h → صفر ارسال |
| ۴ | هر ساعت نظریهٔ دامیننس با چارت ۱ساعته | ✅ | `hamid/dominance_report.py` + ورک‌فلوی ساعتی `dominance-report.yml`: چارت کندلی ۱س USDT.D/BTC.D از سری واقعی اتاق + روند ۱س/۴س + سطوح + سناریوی هر دو جهت + کارنامهٔ پیش‌بینی؛ دادهٔ کهنه = بدون نظریه. محافظ: `test_dom_report` (۹) |
| ۵ | merge نکن؛ اطلاعات کنار هم + شماره‌گذاری | ✅ | قانون ضد-merge در CLAUDE.md؛ آرشیو شماره‌دار append-only (`signals/archive/telegram-sent-*.jsonl` با n پیاپی)؛ همهٔ کارهای امشب کامیت کوچک مستقیم روی main، صفر merge؛ اجتماع وضعیت فقط بر هویت ردیف |
| ۶ | ساختار اصلی پنل و اطلاعات قبلی سالم بماند | ✅ | index.html در تاریخچهٔ اخیر دست‌نخورده (CACHE v21.36)؛ دادهٔ پنل روی gh-pages تازه (۱۲ دقیقه در لحظهٔ بررسی)؛ بک‌آپ کامل قبل از شروع: `backup/main-pre-nightfix-20260826` تأییدشده روی ریموت |
| ۷ | لیست کامل هفته + تیک + اثبات | ✅ | همین سند + همان لیست در پیام پاسخ |
| ۸ | بعد از هر دستور بک‌تست/آزمون | ✅ | بعد از هر کامیت امشب: مجموعهٔ محافظ مربوط اجرا و سبز (test_telegram_dedupe ۱۸ · test_signal_flow ۱۸ · test_single_bot ۶ · test_live_send ۱۳ · test_alert_gate ۲۲ · test_exec_contract ۲۹ · test_pump_radar · test_paper_dedupe ۲۸ · test_workflows ۲۰/۴۶ فایل · test_dom_report ۹) + بک‌تست‌های سنجشی روز (جدول پایین) |
| ۹ | گزارش کامل آزمایش‌های پیپرمود | ✅ | بخش «میز آزمایش‌ها» پایین همین سند + پیام صبح |

## ۲۶ اوت (روز)

| خواسته | وضعیت | اثبات |
|---|---|---|
| بررسی عمیق حجم/ورود پول با متود جدید روی دادهٔ ۳ ساله | ✅ | `hamid/volume_impact.py` (rvol50، rvol فصلی ساعت-روز، CLV، OBV؛ فقط دادهٔ گذشته): همهٔ سطل‌ها −۰.۰۸۵ تا −۰.۱۰۳ هم‌پوشان — حجم به‌تنهایی روی این دفتر لبه نساخت؛ CLV روی ورودی‌های IBS ذاتاً یک‌طرفه است (یافتهٔ روشی، ثبت در E13) |
| دو استراتژی من (شکست سقف/کف ۴س · اردر بلاک) جدا بک‌تست با روند ۴س→۱۵د، RR=3، اهرم ۱۵ | ✅ | `hamid/strategy_duo.py` + دو اجرای کامل روی دادهٔ ۳ ساله: break4h ‏n=۹۴٬۱۵۴ خالص −۰.۰۸۵ CI[−۰.۰۹۶,−۰.۰۷۴] · ob3 ‏n=۶٬۴۶۸ خالص −۰.۱۳۶ CI[−۰.۱۷۷,−۰.۰۹۴] — هر دو با این هندسه زیر صفر؛ فایل‌ها: `brain/research/history/strategy_*.json`؛ محافظ: `test_volume_strategy` (۲۲) |
| منابع آپدیت ایجنت‌ها بررسی شود | ✅ | کتابخانه/قفسه (brain/library)، حلقهٔ مطالعهٔ ۳ساعته، منابع شاهد (TradingView/Polymarket ~۳۰٪ خطا)؛ X هنوز دسترسی ندارد (صادقانه: باز نشده) |
| پیام‌های اشتباه تلگرام قطع شود؛ همه فقط روی یک بات | ✅ | شبه‌سیگنال‌ها در `telegram._log_final` رد می‌شوند؛ پیام‌های «برطرف شد» ساکت (`medic.alert_decision`، ۴۱ بررسی)؛ تک‌بات `@LiamTrader9_Bot` با `test_single_bot` (۶) |
| تصویر فضای کلی ایجنت‌ها | ✅ | نقشهٔ زندهٔ ایجنت‌ها (Artifact) — همان لینک قبلی، دوبار به‌روز شد |
| معماری: گشت صرافی‌ها، نقشهٔ پایهٔ روش من، دیسپچر، A/B، کتابخانهٔ ارز، عیب‌یاب بی‌صدا | ✅/⏳ | گشت: `hamid/scout.py`+`scout.yml` زنده و واچ‌لیست کامیت می‌شود · نقشهٔ پایه: `hamid/base_map.py` (سطح≥۳برخورد، کانال+میدلاین، OB به تعریف حمید، هم‌رسی، حلقهٔ علامت/سناریو/آلارم) · عیب‌یاب بی‌صدا: medic · قانون ۱۱ ثبت شد ⏳ دیسپچر فرمال و تکمیل کتابخانهٔ هر ارز: گام بعد (در نقشهٔ راه قانون ۱۱) |
| حلقهٔ علامت/سناریو/صبر-تا-آلارم («اینجوری به ایجنت توضیح بده») | ✅ | `base_map.reaction_points()` + `stance()` (AT_POINT/WAIT با آلارم دو طرف و سناریوی دو جهت) + تزریق به مهارت `liam-hamid-trading-method`؛ WAIT تصمیم حرفه‌ای است — تحلیل زوری ممنوع |
| نقشهٔ گرافیکی زندهٔ پنل | ✅ | Artifact نقشهٔ ایجنت‌ها (تم تیره، RTL) |
| «پنل ۴۹ دقیقه عقب است — یک بار برای همیشه درستش کن» | ✅ | کلاس عیب: rebase تسلیم‌شونده در ۱۰ ورک‌فلوی ناشر gh-pages → merge -X ours ضدتصادم؛ محافظ کلاس در `test_workflows`؛ راستی‌آزمایی: پنل ۶ دقیقه بعد تازه بود و امشب هم ۱۲ دقیقه |
| «دائم ترید کن و نتیجه و استراتژی‌های جدید رو بگو» | ✅ | پیپر بی‌وقفه ادامه دارد + گزارش کار ۳ نوبت در روز تهران (۰۹/۱۶/۲۲، `work-report.yml`) با میز استراتژی‌های جدید؛ اولین ارسال موفق ثبت شد |
| «مارکت ریخته، شورت بهتره» | ✅ | اندازه‌گیری تأییدش کرد و یک نتیجهٔ قبلی مرا تصحیح کرد: برتری شورت رژیمی است نه ذاتی (۲۰۲۳: شورت −۰.۳۲۰ بدتر از لانگ). برشِ شورتِ ۲۰۲۶ با هندسهٔ گشاد: n=۲۵٬۳۳۵، خالص **+۰.۰۴۶R، CI [+۰.۰۲۳,+۰.۰۶۸] کاملاً بالای صفر** — اولین لبهٔ خالص اثبات‌شدهٔ پروژه؛ نامزد v2.9 منتظر تأیید صریح توست (قانون ۱۲) |

## ۲۵ اوت

| خواسته | وضعیت | اثبات |
|---|---|---|
| «اصلاً متوجه حرفت نمی‌شوم» — گزارش سادهٔ سه‌جمله‌ای | ✅ | قانون گزارش ساده در `00-hamid-personalization.md`؛ همهٔ گزارش‌های خرابی از آن پیروی می‌کنند |
| «این مشکل رو گفتم برای همیشه برطرف کن» (آلارم‌های کهنه) | ✅ | سه بند درس ۲۵ اوت در قانون ۰۷ + `medic.github_health` (فقط «الان قرمز» خرابی است) + `test_medic_alert` (۴۱) |

## ۲۴ اوت

| خواسته | وضعیت | اثبات |
|---|---|---|
| «قبل از مرج همیشه بک‌آپ بگیر» | ✅ | قانون در CLAUDE.md؛ همهٔ بک‌آپ‌ها با list از API راستی‌آزمایی می‌شوند؛ امشب: backup/main-pre-nightfix-20260826 |
| «از بهترین نتایج کد پایتون بساز؛ انتقال مهارت به داشبورد» | ✅ | `hamid/edge_export.py` → `signals/edge.json` (فقط قانون‌های CI-گذشته؛ الان ۱۰ قانون، ۲ تأییدشده)؛ داشبورد v2.8 با sync_all می‌کشد؛ محافظ ۲۳ بررسی |
| میز ۱ دقیقه جدا برود جلو «تا به نتیجه برسد» | ✅/⏹ | تعریف عددی نتیجه: `hamid/scalp_verdict.py`؛ حکم فعلی روی هندسهٔ فعلی: **REJECT** (n=۴٬۲۳۹ · ناخالص +۰.۰۴۰ CI بالای صفر · کارمزد ۰.۲۰۸ · خالص −۰.۱۶۸ CI[−۰.۱۹۴,−۰.۱۴۲]) — لبهٔ ناخالص هست، کارمزد می‌خوردش؛ درمانْ هندسهٔ بزرگ‌تر است نه اهرم |
| موتور قطعی ۱د با دامیننس و همهٔ دروازه‌ها + کانال آپدیت تحلیل | ✅ | `hamid/scalp1m.py` (۱۳ دروازه به ترتیب قانون ۰۰) + `analysis_push.py` (یک‌طرفه، فقط محدودکننده)؛ محافظ ۴۳ بررسی |
| تسویهٔ تکراری دفتر پاک شود | ✅ | ۷۲.۹٪ تکرار کشف و ریشه‌کن شد (trade_key + paper.mark + dedupe_closed)؛ محافظ `test_paper_dedupe` (۲۸)؛ ادعای «تجربه معنادار است» با دفتر پاک پس گرفته شد (تصحیح ثبت‌شده) |
| میز شوک بیت‌کوین | ⏹ | به دستور خودت خاموش (۲۴ اوت): بعد از ۳٬۱۶۳ معامله خالصِ کارمزددار CI شامل صفر؛ کد و آزمون‌ها (۴۶) برای بازآزمایی مانده؛ محافظ چسبندگی نمی‌گذارد کرون بی‌دلیل برگردد |

## ۲۳ اوت

| خواسته | وضعیت | اثبات |
|---|---|---|
| قانون سه‌بار خواندن | ✅ | در CLAUDE.md؛ همین پاسخ آینهٔ چک‌لیست است |
| قوانین سخت ورود اسکلپ ۱د/۳۰ث (۹ قاعده) + اهرم ۱۵–۳۹ + مارجین ۲۵–۳۰٪ | ✅ | قانون ۱۰ + تزریق به مهارت‌های E09/E10/E16/E18/E19؛ ناسازگاری عدد بین مهارت و کد چرخه را سرخ می‌کند (`test_skill_injection`) |
| اصل انباشت تجربه (هضم هر ترید، خواندن رکورد، سنجش با CI) | ✅ | قانون ۰۳ + memory.digest_closed + کارنامهٔ (ارز، جهت) قبل از صدور؛ وضعیت صادقانهٔ اثر تجربه: کل دفتر +۰.۰۹۴ CI[−۰.۱۷,+۰.۳۵] — هنوز از نویز جدا نیست |
| «مرتب پیام می‌آید» — دروازهٔ آلارم با حافظه | ✅ | `alert_gate.py` + DIRECT_OK؛ ۲۲ بررسی |

## ۲۱–۲۲ اوت

| خواسته | وضعیت | اثبات |
|---|---|---|
| نردبان تریل نسخهٔ ۲ (TP1: ⅓ ببند، استاپ به ورود+کارمزد؛ TP2: قفل ۸۵٪) | ✅ | `_exit_plan()` در liam9_strategy + خط 🪜 روی کپشن؛ مرز صادقانه: پایش زنده کار داشبورد/دستی است (قانون ۰۵) |
| کف اهرم اسکلپ ۲۰ (۲۱ اوت) | ⏹ | نسخ شد با دستور ۲۳ اوت (بازهٔ ۱۵–۳۹) |

## ۲۰ اوت

| خواسته | وضعیت | اثبات |
|---|---|---|
| تک‌بات و تک‌گیت‌هاب — مرز قرمز | ✅ | همهٔ آینه‌ها/بات‌های دوم پاک؛ `test_single_bot` در دروازهٔ زنجیره‌ها |
| پامپ فقط ۵ نوبت در روز تهران | ✅ | `pump-review.yml` کرون ۵نوبته؛ پشتوانه: دفتر آلارم n=۳٬۰۹۶ با −۰.۱۸۰R CI زیر صفر؛ محافظ `test_pump_budget` (۱۵) |
| قرارداد اجرا: ایزوله اجباری، استاپ/تارگت اجباری، دروازهٔ جهت بازار، دروازهٔ روند روی ARMED | ✅ | هر سه فایل داشبوردی + `liam9_link.validate_exec` + `scan.gate_stages`؛ محافظ `test_exec_contract` (۲۹) |
| بستهٔ دانش سیگنال-اول (قوانین ۰۷–۰۹، مهارت‌ها، اسکیماها) | ✅ | نصب و مالکیت‌ها داوری شد (E10 مالک L2، E09 هندسهٔ کندل)؛ pre_move_anomaly تا دادهٔ عمق خاموش |

## ۱۹ اوت

| خواسته | وضعیت | اثبات |
|---|---|---|
| قانون شوک BTC + شکار پامپ اهرم ۱۵ + خط زندهٔ امن | ✅/⏹ | پیاده شد (`liam9_shock.py`، `liam9_link.py` با HMAC)؛ میز شوک ۲۴ اوت به دستور خودت خاموش شد (CI خالص شامل صفر)؛ خط امن زنده است |

## ۱۷–۱۸ اوت

| خواسته | وضعیت | اثبات |
|---|---|---|
| قانون رفع قطعی (مشکل بی‌رفع گزارش نشود) | ✅ | همین سند به همان سبک است: هر خرابی با رفع+محافظ+اثبات |
| دروازهٔ روند در گلوگاه ارسال (چارت صعودی، سیگنال شورت ممنوع) | ✅ | `hamid/trend_gate.py` + برچسب «⚠️ خلاف روند»؛ ۷ آزمون + پاسبان C6 |
| استقلال کامل از ریپو/پنل قدیم | ✅ | فقط Auraliam/liam-trader-9؛ دفترها از صفر همین‌جا |
| جایزهٔ انجین‌ها، ناظر دامیننس، ناظر کل E26، فیبوناچی روی IBS، حلقهٔ مطالعه، پایش ۲۰۰ ارز | ✅ | rewards.py · dominance.json (کارنامه‌دار) · overseer.py · premortem.fib_ratio با داوری شبانه · reading_loop.py · live-scan هر ۱۵د |

## ۱۶ اوت

| خواسته | وضعیت | اثبات |
|---|---|---|
| هویت «لیام تریدر ۹» + امضای پنل روی هر پیام | ✅ | `telegram.PANEL_NAME` مرکزی؛ رسید TradingView از فیلد panel؛ بی‌امضا برچسب می‌خورد |
| آستانهٔ پول واقعی بیت‌یونیکس (۶ پیش‌نیاز) | ⏳/✅ | دروازهٔ کارمزد (fees.py) ✅ · کیل‌سوییچ ✅ · سقف سایز ✅ · گذار مرحله‌ای و Reconciliation طراحی‌شده؛ **LIVE_EXECUTION=false می‌ماند** تا تأیید صریح جدا؛ لیست صفر-کارمزد هنوز UNVERIFIED (منبع رسمی نیامده) |
| مرجع خطوط روند (۶ کتاب) + خط فقط ۴س/۱س با ۲۰۰+ کندل و ≥۳ برخورد | ✅ | `trendlines-canon.md` + اصلاح چارت تلگرام؛ چرخهٔ ACTIVE/BROKEN/FLIPPED |
| قانون خواندن پیش از پاسخ | ✅ | در CLAUDE.md؛ رعایت می‌شود |

## میز آزمایش‌های پیپرمود — چه فهمیدند و چه شد (برای گزارش صبح)

| میز | n | نتیجهٔ خالص | حکم |
|---|---|---|---|
| موتور پایه، دفتر ۳ ساله | ۱۵۳٬۹۷۱ | ناخالص +۰.۰۳۲ CI بالای صفر · خالص −۰.۰۹۷ | لبهٔ ناخالص واقعی، کارمزد (۰.۱۲۹R) می‌خوردش |
| هندسهٔ گشاد rr3wide | ۹۴٬۹۶۹ | خالص −۰.۰۴۶ (بهتر از پایه) | مسیر درست: استاپ گشادتر سهم کارمزد را کم می‌کند |
| **rr3wide، فقط شورت، ۲۰۲۶** | **۲۵٬۳۳۵** | **+۰.۰۴۶ · CI [+۰.۰۲۳,+۰.۰۶۸]** | **اولین CI بالای صفرِ خالص — نامزد v2.9، منتظر تأیید حمید** |
| استراتژی break4h (شکست ۴س) | ۹۴٬۱۵۴ | −۰.۰۸۵ CI زیر صفر | با این هندسه رد؛ شورتش (−۰.۰۴۷) از لانگش بهتر — هم‌جهت با یافتهٔ رژیم |
| استراتژی ob3 (اردر بلاک) | ۶٬۴۶۸ | −۰.۱۳۶ CI زیر صفر | رد با این هندسه |
| اسکلپ ۱د (هندسهٔ فعلی) | ۴٬۲۳۹ | −۰.۱۶۸ CI زیر صفر | REJECT ثبت‌شده؛ ناخالص +۰.۰۴۰ مثبت است — درمان هندسهٔ بزرگ‌تر |
| میز شوک | ۳٬۱۶۳ | خالص CI شامل صفر | خاموش به دستور ۲۴ اوت |
| اثر تجربه (دفتر پاک) | ۷۱/۱۲۶ | +۰.۰۹۴ CI[−۰.۱۷,+۰.۳۵] | جهت مثبت، هنوز از نویز جدا نیست — ادعا نمی‌کنیم |
| قفسهٔ لبه (edge.json) | ۱۰ قانون | ۲ قانون CI-گذشته فعال | مثال: «شورت همسو با بیت‌کوین» +۰.۲۴۸ CI[+۰.۰۴,+۰.۴۶] n=۲۲۰ |

درسِ مشترک همهٔ میزها: **لبهٔ ناخالص بارها دیده شده؛ قاتل، نسبت کارمزد به
استاپ است.** هر جا استاپ گشادتر شد، خالص بهتر شد — و تنها جایی که CI خالص
از صفر رد شد، شورتِ رژیمی با هندسهٔ گشاد بود.
