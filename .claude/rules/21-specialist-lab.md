# قانون ۲۱ — میز مستقل ۱۲ متخصص و تعریف قطعی اردر بلاک (دستور حمید، ۱۶ سپتامبر شب)

## دستور

حمید: «هر متخصص با توجه به استراتژی خودش روی ۲۰۰ ارز ۱۰۰۰ ترید در پیپرمود
انجام می‌دهد و اجازهٔ تغییر و آپدیت استراتژی با استفاده از خلاقیت خودش را
دارد و می‌تواند مثل یک تریدر بنشیند چارت و اطلاعات را ببیند و تغییرات را
ایجاد کند و باز ترید کند… هر ارز ۵ ترید، در کل ۱۲ هزار ترید… فقط در
تایم‌فریم ۱۵ دقیقه… می‌تواند در گذشتهٔ ارزها بدون اطلاع از آیندهٔ ارز ترید
کند… اسم متخصصین و نوع استراتژی و اسم استراتژی هر کدام را جلوی اسمشان
بنویس… همه باید بر اساس اعداد واقعی و ترید واقعی و نتایج واقعی باشد و
متخصصین را جوری کدنویسی کن که واقعاً بخواهند نتایج بهتری بگیرند.»

## عیبی که پیش از ساخت پیدا شد (چرا این میز لازم بود)

میز قبلی (`guardian_desk`) به هر ۱۲ مراقب **یک جهانِ ستاپِ مشترک** می‌داد و
آن‌ها فقط رأی می‌دادند. اندازه‌گیری ۱۲ سپتامبر: ثور، حمل، میزان و سنبله
عددِ **دقیقاً یکسان** دادند (n=۳۰۵۳، خالص +۰.۰۱۰۸R، برد ۵۹.۱٪) و عقرب،
جوزا، جدی، قوس و دلو n=۰ از ۱۲٬۷۸۴ ستاپ. یعنی «استراتژی متخصص» وجود
نداشت؛ فقط فیلتری روی ستاپِ کسِ دیگر بود.

## دوازده متخصص — نام · نوع · استراتژی

| متخصص | نوع استراتژی | نام استراتژی | ماشهٔ اصلی |
|---|---|---|---|
| ♏ عقرب | بستر و رژیم | جزر و مد تتر | کندل‌های پرفشارِ هم‌جهتِ پشت‌سرهم |
| ♊ جوزا | همبستگی و لید-لگ | دنباله‌روِ پیشرو | کششِ بیش از حد از میانگین → بازگشت |
| ♉ ثور | دنبال‌کنندهٔ روند | گاوِ روند | دو میانگین هم‌جهت + کلوز بالای هر دو |
| ♈ حمل | شکست و ایمپالس | قوچِ شکست | شکستِ سقف/کف N کندل با بدنهٔ قاطع |
| ♌ اسد | اسمارت مانی (SMC) | اردر بلاکِ حمید | تعریف پایین، ورود روی بازگشت به زون |
| ♋ سرطان | نقدینگی و سوییپ | شکارِ استاپ | ویکِ سوییپ + پس‌گیری در همان کندل |
| ♓ حوت | هندسهٔ کندل | ماهیِ بلعنده | بلعنده/پین‌بارِ **داخل** دامنه، نه شکست |
| ♎ میزان | هندسهٔ ریسک | ترازوی کارمزد | پولبک به میانگین + کفِ استاپ برای مهار کارمزد |
| ♑ جدی | بازگشت به میانگین | بزِ صبور | کششِ بلند + کندلِ برگشتی |
| ♍ سنبله | نوسان و فشردگی | فشردگیِ سنبله | دامنهٔ فشرده → شکستِ همان فشردگی |
| ♐ قوس | شتاب و مومنتوم | کمانِ شتاب | نرخ تغییرِ بالا + حجمِ بالاتر از میانه |
| ♒ دلو | ضدجمعیت | دلوِ خلافِ جمع | حرکتِ پشت‌سرهم با حجمِ رو به کاهش |

پیاده‌سازی: `hamid/specialist_lab.py` (تعریف) · `hamid/specialist_run.py`
(اجرا و بهبود) · `hamid/specialist_report.py` (گزارش فارسی).

## قیدهای سختِ آزمایش

| قید | مقدار | چرا |
|---|---|---|
| تایم‌فریم | فقط ۱۵ دقیقه | دستور صریح |
| ترید بر ارز | ۵ | یک ارزِ پرنوسان نمونه را نبلعد |
| ارز | ۲۰۰ (از استخر بزرگ‌تر تا رسیدن به هدف) | ارزِ بی‌تاریخچهٔ کافی جایگزین می‌شود، نه اینکه هدف کم شود |
| نگاه به آینده | ممنوع | تصمیم از `cd[:i+1]`، نتیجه از i+1 به بعد |
| کارمزد | همیشه کسر می‌شود | هیچ عددِ ناخالصی بی‌خالصش گزارش نمی‌شود |
| ارزِ بلاک‌شده | بیرون از جهان | TRX، دستور ۱۶ سپتامبر |
| برچسب دفتر | `sp-<id>` در `_NOT_SIGNAL` | آمار سیگنالِ ارسالی آلوده نمی‌شود |

## «خلاقیت» چطور کد شد

هر متخصص فضای ایدهٔ خودش را دارد. تاریخ دو نیمه می‌شود: ایده روی **نیمهٔ
اول** انتخاب و روی **نیمهٔ دومِ دیده‌نشده** داوری می‌شود؛ پذیرش فقط اگر
آن‌جا هم بهتر باشد و CI اختلاف کاملاً زیر صفر نباشد. عددِ گزارش‌شده همیشه
از نیمهٔ دوم است. تابع هدف **خالصِ R** است، نه تعداد معامله و نه نرخ برد —
ایده‌ای که تعداد را زیاد و خالص را کم کند رد می‌شود.

## تعریف قطعی اردر بلاک (همان که پنل رعایت نمی‌کرد)

حمید: «اولین کندل رنگ مخالف که بدنهٔ آن از مجموع کل شدوهایش بزرگ‌تر باشد
اردر بلاک است، و برای اثبات آن باید گذشته را در همان تایم‌فریم بررسی کرد
تا مشخص شود واکنشی داشته یا خیر.»

ممیزی ۱۶ سپتامبر: `liam9_strategy.order_block_zone` فقط **رنگِ** کندل را
می‌سنجید — نه بدنه/شدو، نه اثباتِ واکنش؛ زونِ بکر «معتبر» شمرده می‌شد.
`hamid/orderblock.py` از ۱۲ اوت تعریف را درست داشت و پنل از آن استفاده
نمی‌کرد. **کلاسِ عیب: یک مفهوم، دو پیاده‌سازی، یکی بی‌صدا واگرا می‌شود.**

حالا هر دو بند در پنل هست، `proven` روی خروجی می‌نشیند، و محافظ
`hamid/test_orderblock_canon.py` دو پیاده‌سازی را روی ۵۰۰۰ کندل مقایسه
می‌کند؛ اولین اختلاف چرخه را سرخ می‌کند.

## ارزِ بلاک‌شده

`universe.BLOCKED = {"TRX"}` + `is_blocked()` — سه لایه: جهانِ تحلیل
(`backtest.top_symbols`)، گلوگاه ارسال (`telegram.send_signals`)، و هر
دفتر پیپر (`paper.open_from`). این **دستورِ مالک** است نه حکمِ آماری، و
همین‌جا صریح گفته می‌شود تا بعداً «قاعده‌ای با CI» خوانده نشود.

## کجا دیده می‌شود

`signals/specialist-lab.json` (ردیف قرارداد E18) · `signals/specialist-report.md`
(گزارش فارسی) · `brain/specialists/<id>.jsonl` (دفتر append-only هر متخصص).
ورک‌فلو: `specialist-lab.yml` (کرون ۰۳:۱۱ UTC + دستی).

محافظ: `hamid/test_specialist_lab.py` — دوازده استراتژیِ واقعاً متفاوت
(هیچ دو متخصصی ورودِ یکسان یا هم‌پوشانی >۹۰٪ ندارد)، بدون نگاه به آینده،
شناسنامهٔ کامل، داوریِ خارج-از-نمونه، و سیم‌کشی.

## مرز

این دفتر پیپر است: فیل کامل و بی‌لغزش فرض می‌شود، پس سقفِ خوش‌بینانه است
نه انتظارِ لایو. هیچ‌کدام از این دوازده استراتژی وارد تولید نمی‌شود مگر از
مسیر قانون ۰۳ (CI بالای صفر روی نمونهٔ کافی + تأیید صریح حمید).

## تصحیح ۱۷ سپتامبر — جداکنندهٔ برنده از بازنده، با سنجش نه با چشم

گزارش اولِ همین میز گفت «برنده‌ها استاپِ پهن‌تر و سهم کارمزدِ کمتر
دارند». آن جمله از نگاه به سه ردیفِ اولِ جدول درآمده بود، نه از سنجش.
همبستگی روی هر دوازده میز:

| محور | همبستگی با خالصِ R |
|---|---|
| پهنای استاپ (میانه) | **۰.۰۴** — هیچ |
| سهم کارمزد از R (میانه) | **−۰.۱۱** — هیچ |
| نرخ رسیدن به تارگت | **۰.۸۹** |
| نرخ برد | ۰.۶۲ |

شاهدِ نقض در خودِ همان جدول بود و دیده نشد: میزان (استاپ ۱.۳۳۵٪، کارمزد
۰.۱۱۲R) و قوس (۱.۳۰٪، ۰.۱۱۵R) پهن‌ترین استاپ و کم‌ترین کارمزد را دارند
و جزو بدترین‌هایند. آنچه برنده را می‌سازد این است که ستاپ **واقعاً به
تارگت می‌رسد**: جوزا ۳۱٪ · جدی ۳۲٪ · سرطان ۲۵٪، در برابر میزان ۱۱٪ ·
قوس ۱۴٪ · عقرب ۱۶٪.

n=۱۲ میز است، پس این هم جهت است نه قانون. ولی جهتِ قبلی رد شد و
جای‌گزینش از یک سنجشِ بازتولیدپذیر آمد، نه از چشم.

**درسِ روش**: «سه تای اول این‌طوری‌اند» یک مشاهده است، نه یک رابطه.
رابطه را باید روی کلِ جمعیت سنجید — همان چیزی که قانون عددِ درست
(بند ۴، برشِ متقابل) می‌خواهد.

## میز رو-به-جلو — «چقدر روی خودشان اثر گذاشتند؟» (دستور حمید، ۱۷ سپتامبر)

حمید: «گزارش کار متخصصین رو بده که خودشون چقدر روی خودشون تاثیر مثبت
گذاشتن؟» — و معلوم شد میز موجود **نمی‌تواند** این را جواب بدهد.

**چرا نمی‌توانست** (اندازه‌گیری روی دو اجرای ۱۶ سپتامبر ۲۲:۰۸ و ۱۷
سپتامبر ۰۸:۳۵): هر اجرا روی «نیمهٔ دومِ ~۲۱ روز اخیر» قضاوت می‌کند، پس دو
اجرای ۱۰ساعت‌فاصله تقریباً همان داده را می‌بینند.

| سنجه | مقدار |
|---|---|
| میانگین تغییر خالص روی ۱۲ میز | **−۰.۰۱۵۱R** |
| بهتر شده | ۶ از ۱۲ · میانه ~۰ |
| ایده‌های پذیرفته‌شده که در اجرای بعد هم ماندند | **۴ از ۶** |

انتخابی که روی همان داده عوض می‌شود (سرطان و سنبله پس گرفتند، حوت و
عقرب تازه برداشتند) نویز را دنبال می‌کند نه لبه را.

**راه‌حل**: `hamid/specialist_forward.py` — پارامتر هر متخصص **قفل**
می‌شود و از آن لحظه فقط روی کندل‌هایی نمره می‌گیرد که **موقع قفل هنوز
شروع نشده بودند**، در برابر پایهٔ خودِ همان متخصص.

سه قید که این را از «باز هم بهینه‌سازی» جدا می‌کند:

1. **قفل تکان نمی‌خورد.** اجرای فردای آزمایشگاه اگر ایدهٔ دیگری بپسندد،
   به قفل کاری ندارد؛ تنها راه عوض‌شدنش `--refreeze` است. بی این قید،
   پنجرهٔ رو-به-جلو هیچ‌وقت بزرگ نمی‌شد.
2. **بازمحاسبهٔ کامل، نه انباشت** — همان دامی که ۲۴ اوت دفتر پیپر را
   ۷۲.۹٪ باددار کرد و CI را ساختگی تنگ.
3. **مقایسه با پایهٔ خودِ همان متخصص**، نه با بقیه — وگرنه رژیمِ بازار به
   حساب مهارت نوشته می‌شود (نمونهٔ ۱۷ سپتامبر: هر ۱۲ مراقب با هم افتادند،
   −۰.۳۲۵R با CI [−۰.۳۷۶, −۰.۲۷۴] روی n=۲۱۴۹/۳۰۸۴).

قاعدهٔ توقف، ثبت‌شده پیش از دیدن هر عددی: `IMPROVED` با CI خالصِ بالای
صفر روی n≥۲۰۰ (شیداک ۱۲ آزمون، z=۲.۵۸) — فقط *پیشنهاد*؛ `NOT_IMPROVED`
با CI زیر صفر روی n≥۴۰۰؛ بقیه `UNDECIDED`.

خروجی `signals/specialist-forward.json` (ردیف قرارداد، E18) · دفترها
`brain/specialists/frozen.json` و `forward.jsonl` · محافظ
`hamid/test_specialist_forward.py` (۲۱ بررسی؛ اثبات منفی گرفته شد: قفل
را متحرک کردم، سه بررسی افتادند).
